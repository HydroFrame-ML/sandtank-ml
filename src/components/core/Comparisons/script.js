import { mapGetters, mapActions } from "vuex";

// ----------------------------------------------------------------------------
// Component API
// ----------------------------------------------------------------------------

export default {
  name: "Comparisons",
  computed: {
    ...mapGetters({})
  },
  methods: {
    ...mapActions({})
  }
};
